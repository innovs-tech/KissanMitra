spring.application.name=KissanMitra
# ========== SERVER ==========
server.port=8080

# ========== MONGO DATABASE ==========
# MongoDB URI from environment variable (default for local development)
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb+srv://maithanium88:KXTe9fhCzS19aKfk@cluster0.9dguf.mongodb.net/kissanmitra}
spring.data.mongodb.database=kissanmitra
# Enable automatic index creation on application startup
spring.data.mongodb.auto-index-creation=true

# ========== REDIS (for OTP + rate limit) ==========
# Set to false to use in-memory storage (for local development without Redis)
spring.data.redis.enabled=true
spring.data.redis.host=${SPRING_DATA_REDIS_HOST:localhost}
spring.data.redis.port=${SPRING_DATA_REDIS_PORT:6379}
spring.data.redis.timeout=60000

# ========== TWILIO SMS ==========
# Twilio credentials from environment variables (defaults for local development only)
# Set twilio.enabled=false to skip SMS sending (OTP will be logged instead)
# 
# HOW TO FIND YOUR TWILIO CREDENTIALS:
# 1. Go to https://console.twilio.com/
# 2. Account SID & Auth Token: Dashboard > Account Info (top right)
# 3. Phone Number: Phone Numbers > Manage > Active Numbers (use a number from YOUR account)
# 
# IMPORTANT: The 'From' number MUST be a phone number that belongs to YOUR Twilio account
# For trial accounts: You can only send to verified numbers. Verify your number first.
#
twilio.enabled=${TWILIO_ENABLED:true}
twilio.accountSid=${TWILIO_ACCOUNT_SID:ACfc56c97568d41b975f494bd725d0c4f7}
twilio.authToken=${TWILIO_AUTH_TOKEN:your-actual-auth-token-here}
twilio.fromNumber=${TWILIO_FROM_NUMBER:your-twilio-phone-number-here}
# If Twilio fails, allow OTP verification to continue (for development)
twilio.failSilently=${TWILIO_FAIL_SILENTLY:true}


# ========== JWT ==========
# JWT secret from environment variable (default for local development only)
# IMPORTANT: Use strong, unique secret in production via Parameter Store
jwt.secret=${JWT_SECRET:kjsdf8sdf78sdf78sdf78sdf7sdf78sdf7888dfdf8sdf78sdf7sdf==}
jwt.expiration=86400000




# ========= LOGGING OPTIONAL ==========
logging.level.org.springframework=INFO
logging.level.com.uday=DEBUG
spring.jmx.enabled=false
